import React from 'react';

const Finish = props => {
    return (
        <div>
            <h1>You solved it!</h1>
            <button>Play again</button>
        </div>
    );
};

export default Finish;
